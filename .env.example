# Copy to .env for local `netlify dev`

SITE_URL=http://localhost:8888
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Optional override; defaults to ${SITE_URL}/auth/callback
# GOOGLE_REDIRECT_URI=http://localhost:8888/auth/callback
SESSION_SECRET=replace-with-a-long-random-string-at-least-32-chars
# Optional: allow manual invoke of weekly-allowance with Authorization: Bearer …
# CRON_SECRET=
